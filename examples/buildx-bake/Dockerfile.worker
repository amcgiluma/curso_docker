# syntax=docker/dockerfile:1

FROM alpine:3.20
ARG TARGETPLATFORM
ARG BUILDPLATFORM
RUN echo "Construyendo worker en ${BUILDPLATFORM} para ${TARGETPLATFORM}"
CMD ["sh", "-c", "echo worker listo en $(uname -m)"]
