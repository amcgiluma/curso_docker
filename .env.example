# Copia este fichero a .env y ajusta los valores segun tu entorno.

# Directorio del contenido del curso (dentro del contenedor del backend).
CONTENT_DIR=/content

# Origenes permitidos por CORS en el backend (separados por comas).
CORS_ORIGINS=http://localhost:8080,http://localhost:5173
